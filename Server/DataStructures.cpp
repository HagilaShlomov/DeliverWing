#include "DataStructures.h"
#include "Order.h"
#include "Coordinate.h"
#include <unordered_set>
#include <cmath>
#include <unordered_map>
#include "User.h" // Ensure the User class is included in the file where it's used
#include <map> // Include the correct header for std::map

std::map<int, User> usersMap;

std::unordered_set<Order> addressSet;


