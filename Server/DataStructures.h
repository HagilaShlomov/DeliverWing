#pragma once
#include <unordered_set>
#include <queue> // Include this header for std::priority_queue
#include <vector>
#include <functional>
#include <string>
#include "Order.h"
#include "Coordinate.h"
#include "Node.h"


using namespace std;

	// Priority queue for the open list
	/*using PriorityQueue = priority_queue<Node*, std::vector<Node*>, std::function<bool(Node*, Node*)>>;

	using OpenList = std::priority_queue<Node, std::vector<Node>, std::greater<Node>>;

	using ClosedSet = unordered_set<Coordinate>;

	extern std::unordered_set<Address> addressSet;*/



